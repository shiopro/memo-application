<ul>
  <% @memos.each do |memo| %>
    <li>
      <a href="/memos/<%= memo["id"] %>"><%= CGI.escapeHTML(memo["title"]) %></a>
    </li>
  <% end %>
</ul>
<div class="container">
  <a class="btn-primary" href="/memos/new">追加</a>
</div>
